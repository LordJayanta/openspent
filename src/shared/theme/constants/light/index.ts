import { TYPOGRAPHY } from "../../shared/typography";
import { COLORS } from "./colors";

const meta = {
  name: "light",
  themeId: "light",
  version: "1.0.0",
};

export { COLORS, meta, TYPOGRAPHY };

